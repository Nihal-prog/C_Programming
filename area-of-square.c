/* Using the <math.h> library to calculate the area of a square. */

#include <stdio.h>
#include <math.h>

int main(){
    int a = 5;
    printf("Area of square with side 5 is: %f.\n", pow(a, 2));
    return 0;
}